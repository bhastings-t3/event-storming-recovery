# build artifacts from the local static-serve verify step
**/serve.js
.playwright-cli/
node_modules/
*.log

# built SPA (regenerated by `npm run build`; shipped in the npm package via package.json "files")
dist/

# npm pack artifacts
*.tgz

# explorer comment sidecars (human notes; live next to the model, not in source control here)
comments.json
