Closes #

<!--
The three lenses from docs/process/review.md. Mechanical checks are CI's job and
are not a lens; do not report them here.

An empty section means the lens was skipped. Write "not applicable, docs only"
rather than leaving it blank.
-->

## Functionality

<!-- What you ran, what you clicked, what you saw. Drive the app (npm run dev, or
     the real CLI/MCP subcommand), not the tests. Include the happy path, one
     realistic failure path, and anything that broke. "Tests pass" is not an
     answer to this section. -->

## Code

<!-- What this does, in your own words. Concerns, if any. -->

## Architecture

<!-- New patterns introduced, dependencies added and what they cost to remove
     later, duplication found or avoided. Note any ADR added. Is the model still
     the single source of truth? Is the domain/application -> adapters boundary
     still intact? -->

## Not done

<!-- Anything in the issue you did not do, and why. Including any check you
     could not make pass: say so here rather than disabling it quietly. -->
