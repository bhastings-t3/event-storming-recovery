# build artifacts from the local static-serve verify step
**/serve.js
.playwright-cli/

# playwright-bdd E2E harness (#19): generated specs + Playwright run output
.features-gen/
playwright-report/
test-results/
blob-report/
node_modules/
*.log

# parallel-session git worktrees (never part of a commit)
.claude/worktrees/

# per-user local Claude Code settings (shared settings live in .claude/settings.json)
.claude/settings.local.json

# built SPA (regenerated by `npm run build`; shipped in the npm package via package.json "files")
dist/

# npm pack artifacts
*.tgz

# explorer comment sidecars (human notes; live next to the model, not in source control here)
comments.json
